read:
  mode: "proto"
  proto:
      pathToDescriptor: "PATH_TO_PROTO_FILE"
  subject: "*"

server:
  port: 1111

logging:
  level:
    org.jetstreamDrop.monitoring.NatsMonitoringApiClient: DEBUG

nats:
  server:
#    monitoring:      # Optional, monitoring needs to be enabled in the nats server
#      port: 8222
#      protocol: "http"
    host: "localhost"
    port: 4222